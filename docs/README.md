# VISTA Architect — Documentation

| File | Purpose |
|---|---|
| `architecture.md` | High-level system architecture (two-tier graph, agentic AI bridge, generative UI) |
| `deterministic_retrieval.md` | Reference for the deterministic retrieval layer (`toa/deterministic_retrieval.py`) — keyword/regex helpers over the MEDS Graph |
| `graph_guided_retrieval.md` | How graph traversal is used to navigate from TOA events back to source EHR evidence |
| `agentic_pipeline.md` | Detailed design and per-stage walkthrough of the agentic build pipeline (`agentic_toa/`) |

For a one-page summary of the system, see the project root `README.md`.
For backend setup, see `../SETUP.md`.
